const { gql } = require("apollo-server");

// 🐨 Copy the typeDefs from the monolith/User/typeDefs.js.
// 🐨 Comment out the reviews field as we don't have reviews type defined here yet, it won't be necessary for the first step to pass.

exports.typeDefs = gql``;
